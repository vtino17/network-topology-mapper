[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "network-topology-mapper"
version = "1.0.0"
description = "ARP-based network topology discovery"
requires-python = ">=3.10"
